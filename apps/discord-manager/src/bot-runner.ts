import { Client,GatewayIntentBits } from 'discord.js'; import type Redis from 'ioredis'; import type { ApiClient,BotSummary } from './api-client.js'; import { RedisBotLock } from './redis-lock.js';
export class BotRunner {private client:Client|null=null;private timer:NodeJS.Timeout|null=null;private readonly lock:RedisBotLock;constructor(private readonly bot:BotSummary,private readonly api:ApiClient,redis:Redis,private readonly workerId:string,ttlMs:number){this.lock=new RedisBotLock(redis,bot.id,ttlMs);}async start():Promise<boolean>{if(!await this.lock.acquire())return false;try{const credentials=await this.api.credentials(this.bot.id);this.client=new Client({intents:[GatewayIntentBits.Guilds,GatewayIntentBits.GuildVoiceStates]});this.client.once('ready',()=>void this.heartbeat('online'));this.client.on('error',error=>void this.heartbeat('error',error.message));await this.client.login(credentials.token);this.timer=setInterval(()=>void this.tick(),Math.floor(20_000));return true;}catch(error){await this.lock.release();throw error;}}private async tick():Promise<void>{if(!await this.lock.renew()){await this.stop();return;}await this.heartbeat(this.client?.isReady()?'online':'connecting');}private heartbeat(status:string,error?:string):Promise<{accepted:boolean}>{return this.api.heartbeat(this.bot.id,{worker_id:this.workerId,hostname:process.env.HOSTNAME??'unknown',status,version:'0.1.0',...(error===undefined?{}:{error})});}async stop():Promise<void>{if(this.timer)clearInterval(this.timer);this.client?.destroy();this.client=null;await this.lock.release();}}
