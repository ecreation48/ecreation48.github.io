import {describe,expect,it,vi} from 'vitest'; import {RedisBotLock} from './redis-lock.js';
describe('RedisBotLock',()=>{it('uses an exclusive expiring key',async()=>{const redis={set:vi.fn().mockResolvedValue('OK'),eval:vi.fn().mockResolvedValue(1)};const lock=new RedisBotLock(redis as never,'bot-1',30000);expect(await lock.acquire()).toBe(true);expect(redis.set).toHaveBeenCalledWith('discord-bot-lock:bot-1',expect.any(String),'PX',30000,'NX');expect(await lock.renew()).toBe(true);await lock.release();expect(redis.eval).toHaveBeenCalledTimes(2);});it('reports contention',async()=>{const redis={set:vi.fn().mockResolvedValue(null)};expect(await new RedisBotLock(redis as never,'bot-1',30000).acquire()).toBe(false);});});
