export interface BotSummary {id:string;name:string;client_id:string;connection_status:string}
interface ApiEnvelope<T>{data:T}
export class ApiClient {constructor(private readonly baseUrl:string,private readonly token:string){} private async request<T>(path:string,init:RequestInit={}):Promise<T>{const response=await fetch(`${this.baseUrl}${path}`,{...init,headers:{authorization:`Bearer ${this.token}`,'content-type':'application/json',...init.headers},signal:AbortSignal.timeout(10_000)});if(!response.ok)throw new Error(`Internal API ${response.status} on ${path}`);return (await response.json() as ApiEnvelope<T>).data;} listBots():Promise<BotSummary[]>{return this.request('/bots');} credentials(id:string):Promise<{id:string;token:string;client_id:string}>{return this.request(`/bots/${id}/credentials`);} heartbeat(id:string,payload:Record<string,string>):Promise<{accepted:boolean}>{return this.request(`/bots/${id}/heartbeat`,{method:'POST',body:JSON.stringify(payload)});} }
